# دليل المطور الشامل: تحديث ملفات BarberFlow Pro (Post-Supabase)

هذا الملف هو المرجع الأساسي والقواعد الصارمة التي يجب اتباعها عند تحديث أو إنشاء أي ملف في مشروع BarberFlow Pro بعد التحويل من Firebase إلى Supabase.

---

## 1. تعريف المنصة

**BarberFlow Pro** هي منصة رقمية متكاملة لقطاع الحلاقة والتجميل، تربط بين ثلاثة أطراف رئيسية:

- **الصالونات**: لإدارة المواعيد، الموظفين، الفروع، الخدمات، والمخزون.
- **المتاجر**: لبيع منتجات العناية والحلاقة وإدارة الطلبات.
- **الزبائن**: لحجز المواعيد، شراء المنتجات، وتقييم الخدمات.

---

## 2. القواعد الإلزامية لتحديث أي ملف

### أ. توحيد التنسيقات (CSS)

- يجب أن تعتمد جميع ملفات CSS الفرعية بشكل كلي على المتغيرات المعرفة في `shared/styles/global.css`.
- هذا يضمن قدرة النظام على التحكم بالثيمات (فاتح/داكن) وضمان تجاوب الموقع مع جميع أحجام الشاشات دون تكرار الكود.

### ب. الالتزام بالملفات المشتركة وملفات الحماية

- **يُمنع منعاً باتاً** إعادة كتابة الأكواد المشتركة داخل الصفحات الفردية.
- يجب استدعاء الملفات من مجلدات `shared` و `middleware` عند الحاجة إليها فقط.
- يتم تحديث هذه الملفات المركزية بدلاً من تعديل كل صفحة على حدة.

### ج. قاعدة البيانات: Supabase حصراً

- تم استبدال Firebase تماماً بـ Supabase.
- يجب استخدام `config/supabase-init.js` للاتصال.
- استخدام دوال Supabase للمصادقة وقاعدة البيانات والتخزين.
- **لا يُسمح باستخدام أي مكتبات تابعة لـ Firebase**.

### د. توحيد أسماء المتغيرات والحقول

- يجب تطابق أسماء المتغيرات في كود JavaScript/TypeScript تماماً مع أسماء الحقول المخزنة في قاعدة بيانات Supabase.
- ما يتم تخزينه في قاعدة البيانات هو نفس الاسم الذي يتم جلبه أو التعديل عليه في الواجهة الأمامية.
- يمنع هذا الاختلاف تجنب الأخطاء المنطقية والبحث عن حقول بأسماء غير موجودة.

### هـ. الثيم الافتراضي

- **الثيم الفاتح (Light) هو الافتراضي** عند فتح الموقع لأول مرة.
- الثيم الداكن (Dark) اختياري ويُفعّل فقط عند اختيار المستخدم له.
- عند تطبيق الثيم:
  - **الفاتح**: إزالة `data-theme` من `<html>`
  - **الداكن**: إضافة `data-theme="dark"` إلى `<html>`

### و. تخزين الصور

- **لم نعد نحتاج لتحويل الصور إلى base64**.
- نستخدم **Supabase Storage** لرفع الصور مباشرة.
- استخدم `shared/utils/images-utils.js` لجميع عمليات رفع وحذف الصور.

---

## 3. مرجع قاعدة البيانات (Supabase Schema Reference)

فيما يلي الجداول الأساسية وأسماء الحقول الدقيقة التي يجب استخدامها في الكود (`select`, `insert`, `update`).

### 3.1 جدول `profiles` (الملفات الشخصية للمستخدمين)

يرتبط مباشرة بجدول المصادقة `auth.users`. يتم إنشاؤه تلقائياً عبر Trigger عند التسجيل.

| اسم الحقل (Field Name) | النوع (Type) | الوصف والاستخدام في الكود |
|------------------------|--------------|---------------------------|
| `id` | UUID | المعرف الرئيسي. يساوي `user.uid` من المصادقة. |
| `full_name` | TEXT | الاسم الكامل للمستخدم. يستخدم في عرض البروفايل. |
| `role` | ENUM | دور المستخدم. القيم المسموحة: `'customer'`, `'salon'`, `'store'`. |
| `phone` | TEXT | رقم الهاتف (فريد). يستخدم لتسجيل الدخول أو التواصل. |
| `avatar_url` | TEXT | رابط صورة الملف الشخصي. |
| `onboarding_status` | ENUM | حالة الإعداد الأولي. القيم: `'empty'`, `'incomplete'`, `'completed'`. |
| `created_at` | TIMESTAMPTZ | تاريخ الإنشاء (تلقائي). |
| `updated_at` | TIMESTAMPTZ | تاريخ آخر تعديل (تلقائي). |

⚠️ **ملاحظة**: عند التسجيل، يجب تمرير `full_name` و `role` داخل `options.data` في دالة `supabase.auth.signUp` ليتم التقاطها بواسطة الـ Trigger وإنشاء السجل في هذا الجدول.

### 3.2 جدول `businesses` (الشركات / الصالونات / المتاجر)

يمثل الكيان التجاري الرئيسي.

| اسم الحقل (Field Name) | النوع (Type) | الوصف والاستخدام في الكود |
|------------------------|--------------|---------------------------|
| `id` | UUID | معرف النشاط التجاري. |
| `owner_id` | UUID | معرف المالك (يرتبط بـ `profiles.id`). |
| `name` | TEXT | اسم الصالون أو المتجر. |
| `type` | ENUM | نوع النشاط: `'salon'` أو `'store'`. |
| `description` | TEXT | وصف تفصيلي للنشاط. |
| `city` | TEXT | المدينة. |
| `address` | TEXT | العنوان التفصيلي. |
| `phone` | TEXT | هاتف التواصل الخاص بالنشاط. |
| `email` | TEXT | البريد الإلكتروني الرسمي للنشاط. |
| `logo_url` | TEXT | رابط شعار النشاط. |
| `cover_url` | TEXT | رابط صورة الغلاف. |
| `working_hours` | JSONB | أوقات العمل. مثال: `{"open": "09:00", "close": "21:00", "days": ["sun","mon"]}` |
| `status` | ENUM | حالة النشاط: `'inactive'`, `'active'`, `'suspended'`. |
| `is_verified` | BOOLEAN | هل تم توثيق الحساب؟ |
| `rating` | DECIMAL | متوسط التقييم (0.00 - 5.00). |
| `reviews_count` | INT | عدد التقييمات الكلي. |

### 3.3 جدول `branches` (الفروع)

يدعم تعدد الفروع للصالونات والمتاجر.

| اسم الحقل (Field Name) | النوع (Type) | الوصف والاستخدام في الكود |
|------------------------|--------------|---------------------------|
| `id` | UUID | معرف الفرع. |
| `business_id` | UUID | يرتبط بجدول `businesses`. |
| `name` | TEXT | اسم الفرع. |
| `city` | TEXT | مدينة الفرع. |
| `address` | TEXT | عنوان الفرع. |
| `phone` | TEXT | هاتف الفرع. |
| `manager_id` | UUID | مدير الفرع (يرتبط بـ `profiles.id`). |
| `working_hours` | JSONB | أوقات عمل الفرع (يمكن أن تختلف عن الرئيسي). |
| `status` | ENUM | حالة الفرع: `'active'`, `'inactive'`. |

### 3.4 جدول `salon_stores` (متاجر الصالونات - اختياري)

يسمح للصالون ببيع منتجاته.

| اسم الحقل (Field Name) | النوع (Type) | الوصف والاستخدام في الكود |
|------------------------|--------------|---------------------------|
| `id` | UUID | معرف متجر الصالون. |
| `salon_id` | UUID | يرتبط بجدول `businesses` (حيث type='salon'). فريد لكل صالون. |
| `name` | TEXT | اسم المتجر (افتراضي: 'متجر الصالون'). |
| `description` | TEXT | وصف المتجر. |
| `status` | ENUM | حالة المتجر: `'inactive'`, `'active'`. |

### 3.5 جدول `products` (المنتجات)

للمنتجات العامة أو منتجات المتاجر/صالونات.

| اسم الحقل (Field Name) | النوع (Type) | الوصف والاستخدام في الكود |
|------------------------|--------------|---------------------------|
| `id` | UUID | معرف المنتج. |
| `seller_id` | UUID | معرف المتجر المستقل (يرتبط بـ `businesses`). |
| `salon_store_id` | UUID | معرف متجر الصالون (يرتبط بـ `salon_stores`). |
| `name` | TEXT | اسم المنتج. |
| `description` | TEXT | وصف المنتج. |
| `price` | DECIMAL | سعر المنتج. |
| `stock_quantity` | INT | كمية المخزون المتاحة. |
| `image_url` | TEXT | رابط صورة المنتج. |
| `category` | TEXT | تصنيف المنتج. |
| `is_available` | BOOLEAN | هل المنتج متاح للبيع؟ |

### 3.6 جدول `services` (الخدمات)

خاصة بالصالونات وفروعها.

| اسم الحقل (Field Name) | النوع (Type) | الوصف والاستخدام في الكود |
|------------------------|--------------|---------------------------|
| `id` | UUID | معرف الخدمة. |
| `business_id` | UUID | يرتبط بجدول `businesses` (حيث type='salon'). |
| `branch_id` | UUID | الفرع المرتبط (NULL تعني جميع الفروع). |
| `name` | TEXT | اسم الخدمة. |
| `description` | TEXT | وصف الخدمة. |
| `price` | DECIMAL | سعر الخدمة. |
| `duration_min` | INT | مدة الخدمة بالدقائق. |
| `category` | TEXT | تصنيف الخدمة. |
| `is_available` | BOOLEAN | هل الخدمة متاحة للحجز؟ |

### 3.7 جدول `bookings` (الحجوزات)

| اسم الحقل (Field Name) | النوع (Type) | الوصف والاستخدام في الكود |
|------------------------|--------------|---------------------------|
| `id` | UUID | معرف الحجز. |
| `customer_id` | UUID | العميل الحاجز (يرتبط بـ `profiles`). |
| `service_id` | UUID | الخدمة المحجوزة (يرتبط بـ `services`). |
| `branch_id` | UUID | الفرع المحجوز فيه (NULL = الفرع الرئيسي). |
| `staff_id` | UUID | الموظف المنفذ (يرتبط بـ `profiles`). |
| `booking_date` | DATE | تاريخ الحجز. |
| `start_time` | TIME | وقت البدء. |
| `end_time` | TIME | وقت الانتهاء. |
| `status` | TEXT | حالة الحجز: `'pending'`, `'confirmed'`, `'completed'`, `'cancelled'`. |
| `notes` | TEXT | ملاحظات إضافية. |

### 3.8 جدول `reviews` (التقييمات)

| اسم الحقل (Field Name) | النوع (Type) | الوصف والاستخدام في الكود |
|------------------------|--------------|---------------------------|
| `id` | UUID | معرف التقييم. |
| `reviewer_id` | UUID | صاحب التقييم (يرتبط بـ `profiles`). |
| `business_id` | UUID | النشاط المُقيَّم (صالون/متجر). |
| `branch_id` | UUID | الفرع المُقيَّم (اختياري). |
| `product_id` | UUID | المنتج المُقيَّم (اختياري). |
| `rating` | INT | التقييم (1-5). |
| `comment` | TEXT | نص التقييم. |
| `reply` | TEXT | رد المالك على التقييم. |
| `replied_at` | TIMESTAMPTZ | تاريخ الرد. |

### 3.9 جدول `favorites` (المفضلة) - 🆕 جديد

| اسم الحقل (Field Name) | النوع (Type) | الوصف والاستخدام في الكود |
|------------------------|--------------|---------------------------|
| `id` | UUID | معرف المفضلة. |
| `user_id` | UUID | المستخدم (يرتبط بـ `profiles`). |
| `item_id` | UUID | معرف العنصر (صالون/منتج/خدمة). |
| `item_type` | TEXT | نوع العنصر: `'salon'`, `'product'`, `'service'`. |
| `created_at` | TIMESTAMPTZ | تاريخ الإضافة. |

⚠️ **قيود فريدة**: `UNIQUE(user_id, item_id, item_type)` لمنع التكرار.

### 3.10 جدول `subscriptions` (الاشتراكات) - 🆕 جديد

| اسم الحقل (Field Name) | النوع (Type) | الوصف والاستخدام في الكود |
|------------------------|--------------|---------------------------|
| `id` | UUID | معرف الاشتراك. |
| `user_id` | UUID | المستخدم (يرتبط بـ `profiles`). |
| `plan` | TEXT | الباقة: `'starter'`, `'professional'`, `'enterprise'`. |
| `status` | TEXT | الحالة: `'active'`, `'expired'`, `'cancelled'`. |
| `start_date` | DATE | تاريخ بداية الاشتراك. |
| `end_date` | DATE | تاريخ انتهاء الاشتراك. |
| `features` | JSONB | الميزات المفعّلة. مثال: `{"analytics": true, "multi_branch": true}` |
| `created_at` | TIMESTAMPTZ | تاريخ الإنشاء. |

---

## 4. هيكلية الملفات الكاملة (بناءً على الهيكلية الجديدة)

### مجلد `config/`

- **`supabase-init.js`**: تهيئة عميل Supabase والاتصال بقاعدة البيانات والمصادقة.
  - المسار: `config/supabase-init.js`
  - الدور: نقطة الدخول الوحيدة للاتصال بالخلفية.

### مجلد `auth/` (صفحات المصادقة والترحيب)

- **`login.html/.css/.js`**: صفحة تسجيل الدخول (بريد/هاتف/Google).
- **`register.html/.css/.js`**: صفحة إنشاء حساب جديد مع اختيار الدور.
- **`forgot-password.html/.css/.js`**: استعادة كلمة المرور عبر البريد.
- **`reset-password.html/.css/.js`**: إعادة تعيين كلمة المرور الجديدة.
- **`verify-email.html/.css/.js`**: تأكيد البريد الإلكتروني.
- **`welcome.html/.css/.js`**: صفحة الترحيب الديناميكية وحارس التوجيه. ✅ تم النقل هنا.

### مجلد `billing/` (الفواتير والمدفوعات)

- **`checkout.html/.css/.js`**: صفحة إتمام الدفع.
- **`payment-success.html/.css/.js`**: صفحة نجاح الدفع.
- **`payment-cancel.html/.css/.js`**: صفحة إلغاء الدفع.
- **`subscription.html/.css/.js`**: إدارة الاشتراكات.

### مجلد `dashboard/` (لوحة التحكم الإدارية)

- **`index.html/.css/.js`**: الصفحة الرئيسية للوحة التحكم.
- **`analytics.html/.css/.js`**: صفحة التحليلات والتقارير.
- **`appointments.html/.css/.js`**: إدارة الحجوزات والمواعيد.
- **`notifications.html/.css/.js`**: مركز الإشعارات.
- **`reviews.html/.css/.js`**: إدارة التقييمات.
- **`customers/`**: مجلد إدارة الزبائن.
- **`orders/`**: مجلد إدارة الطلبات.
- **`products/`**: مجلد إدارة المنتجات.
- **`services/`**: مجلد إدارة الخدمات.
- **`settings/`**: مجلد الإعدادات (`settings-general.*`, `settings-salon.*`, `settings-store.*`).
- **`staff/`**: مجلد إدارة الموظفين.

### مجلد `middleware/` (الطبقة الوسطى والحماية)

- **`index.js`**: نقطة التصدير المركزية لجميع دوال الـ Middleware.

#### `middleware/auth/`
- **`auth-state.js`**: إدارة حالة المصادقة (Supabase).
- **`index.js`**: التصدير المركزي.

#### `middleware/guards/`
- **`booking-guard.js`**: حارس الحجوزات - التحقق من توفر الأوقات ومنع التكرار.
- **`role-guard.js`**: نظام التحقق من الصلاحيات والأدوار.
- **`subscription-route-guard.js`**: 🆕 حماية الصفحات المدفوعة حسب الباقة.
- **`index.js`**: التصدير المركزي.

#### `middleware/routing/`
- **`page-guard.js`**: نظام حماية الصفحات ومنع الومضة (FOUC).
- **`page-router.js`**: نظام التوجيه والحماية المركزي.
- **`profile-route.js`**: التوجيه الذكي لملفات المستخدمين حسب أدوارهم.
- **`index.js`**: التصدير المركزي.

#### `middleware/subscription/`
- **`subscription-guard.js`**: للتحقق من صلاحية الاشتراك قبل الوصول للمميزات.
- **`index.js`**: التصدير المركزي.

#### `middleware/validation/`
- **`images-sanitizer.js`**: نظام فلترة وحماية الصور قبل الرفع إلى Supabase Storage.
- **`input-sanitizer.js`**: تنظيف والتحقق من المدخلات (يشمل `isValidMoroccanPhone`).
- **`index.js`**: التصدير المركزي.

### مجلد `onboarding/` (الإرشاد الأولي)

- **`add-customer/salon/store.html/.css/.js`**: صفحات إضافة البيانات الأولية.
- **`setup-customer/salon/store.html/.css/.js`**: صفحات إعداد الحساب.

### مجلد `profile/` (الملفات الشخصية العامة)

- **`customer.html/.css/.js`**: بروفايل الزبون.
- **`salon.html/.css/.js`**: بروفايل الصالون العام.
- **`store.html/.css/.js`**: بروفايل المتجر العام.

### مجلد `public/` (الملفات الثابتة والأصول)

- **`assets/`**: مجلد الصور، الأيقونات، والخطوط.
- **`manifest.json`**: ملف تعريف تطبيق الويب (PWA).
- **`robots.txt`**: تعليمات لمحركات البحث.
- **`sitemap.xml`**: خريطة الموقع للأرشفة.

### مجلد `shared/` (المكونات والأدوات المشتركة)

#### `shared/components/` (بطاقات العرض)

- **`card-salon.js`**: مكون بطاقة الصالون (Supabase + favorites table).
- **`card-store.js`**: مكون بطاقة المنتج/المتجر (Supabase + favorites table).
- **`card-offer.js`**: مكون بطاقة العرض.
- **`card-concierge.js`**: مكون بطاقة الكونسيرج (الخدمات المنزلية).
- **`card-staff.js`**: 🆕 مكون بطاقة الموظف/الحلاق.
- **`card-service.js`**: 🆕 مكون بطاقة الخدمة.
- **`card-review.js`**: 🆕 مكون بطاقة التقييم.
- **`card-booking.js`**: 🆕 مكون بطاقة الحجز.

#### `shared/layout/`

- **`global-navbar.html`**: هيكل شريط التنقل.
- **`global-navbar.js`**: منطق شريط التنقل (Supabase + الثيم الفاتح افتراضي).

#### `shared/styles/` (التنسيقات العالمية)

- **`global.css`**: المتغيرات العامة والأنماط الأساسية (الثيم الفاتح افتراضي).
- **`global-navbar.css`**: تنسيقات شريط التنقل (الثيم الفاتح افتراضي).
- **`cards.css`**: تنسيقات جميع البطاقات (يشمل البطاقات الجديدة).
- **`notifications.css`**: تنسيقات نظام التنبيهات (الثيم الفاتح افتراضي).
- **`page-protection.css`**: تنسيقات شاشة التحميل (اللوغو في الوسط مع حلقات دوّارة).

#### `shared/utils/` (الأدوات المساعدة)

- **`analytics.js`**: تحليلات سلوك المستخدم.
- **`debounce.js`**: حماية من النقرات المتكررة.
- **`images-utils.js`**: 🔄 أدوات رفع وإدارة الصور في Supabase Storage (بدلاً من base64).
- **`notifications.js`**: نظام التنبيهات الموحد.
- **`paths.js`**: مركزية جميع مسارات المشروع.
- **`user-preferences.js`**: تفضيلات المستخدم (الثيم الفاتح افتراضي).
- **`error-handler.js`**: 🆕 إدارة الأخطاء المركزية.
- **`cache.js`**: 🆕 نظام التخزين المؤقت للبيانات.
- **`date-utils.js`**: 🆕 أدوات التواريخ والأوقات (تنسيق عربي).
- **`index.js`**: التصدير المركزي لجميع الأدوات.

### ملفات الجذر (Root Files)

- **`.gitignore`**: تجاهل الملفات غير المرغوبة في Git.
- **`404.html/.css/.js`**: صفحة الخطأ 404 المخصصة.
- **`about.html/.css/.js`**: صفحة "من نحن".
- **`booking.html/.css/.js`**: صفحة الحجز العامة.
- **`contact.html/.css/.js`**: صفحة التواصل معنا.
- **`details-salon/store.html/.css/.js`**: صفحات تفاصيل الصالون/المتجر.
- **`faq.html/.css/.js`**: صفحة الأسئلة الشائعة.
- **`index.html/.css/.js`**: الصفحة الرئيسية للمنصة.
- **`guide.md`**: هذا الملف (دليل القواعد).
- **`privacy.html/.css/.js`**: سياسة الخصوصية.
- **`pro.html/.css/.js`**: صفحة العروض الاحترافية.
- **`product.html/.css/.js`**: صفحة تفاصيل المنتج.
- **`README.md`**: ملف readme للمشروع.
- **`salons.html/.css/.js`**: صفحة تصفح الصالونات.
- **`shop.html/.css/.js`**: صفحة المتجر العام.
- **`survey.html/.css/.js`**: صفحة الاستبيانات.
- **`terms.html/.css/.js`**: شروط الاستخدام.

---

## 5. الملفات الجديدة المضافة (🆕)

### 5.1 `middleware/guards/subscription-route-guard.js`

**الدور**: حماية الصفحات المدفوعة حسب الباقة.

**الدوال الرئيسية**:
- `requireActiveSubscription(requiredPlan, redirectPath)`: التحقق من أن المستخدم يملك اشتراك نشط.
- `requireFeature(feature, redirectPath)`: التحقق من أن المستخدم يملك ميزة معينة.
- `protectElement(elementId, requiredPlan, featureName)`: قفل عنصر في الصفحة لغير المشتركين.

**مثال الاستخدام**:

```javascript
import { requireActiveSubscription } from '../middleware/guards/subscription-route-guard.js';

document.addEventListener('DOMContentLoaded', async () => {
    const allowed = await requireActiveSubscription('professional');
    if (!allowed) return; // سيتم التوجيه تلقائياً
    
    // تحميل المحتوى المحمي...
});

